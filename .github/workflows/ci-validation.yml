name: CI Validation

# El "Robot Inspector": valida cada Pull Request antes de la revision humana.
# Compila (typecheck) backend y frontend. Lint/tests corren solo si existe config,
# para no bloquear el flujo mientras el proyecto esta en andamiaje.

on:
  # En los PR hacia las ramas protegidas: es el check obligatorio para poder fusionar.
  pull_request:
    branches: [main, qa, develop]
  # Y tambien en CADA push a una rama de trabajo, aunque todavia no exista el PR.
  # Sin esto, empujar a feature/* no disparaba nada y los cambios llegaban sin validar.
  push:
    branches-ignore: [main, qa, develop]

# Si se sube un commit nuevo, cancela la corrida anterior de la misma rama/PR.
concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend - typecheck & build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Instalar dependencias
        run: npm install
      - name: Compilar / typecheck (tsc)
        run: npm run build
      - name: Lint (solo si hay config de ESLint)
        shell: bash
        run: |
          if ls .eslintrc* eslint.config.* >/dev/null 2>&1; then
            npm run lint
          else
            echo "Sin config de ESLint en backend: se omite lint."
          fi

  frontend:
    name: Frontend - typecheck & build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Instalar dependencias
        run: npm install
      - name: Compilar (tsc + vite build)
        run: npm run build
      - name: Lint (solo si hay config de ESLint)
        shell: bash
        run: |
          if ls .eslintrc* eslint.config.* >/dev/null 2>&1; then
            npm run lint
          else
            echo "Sin config de ESLint en frontend: se omite lint."
          fi
